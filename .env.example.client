# Client environment variables for send-test-fax.ts CLI script

# Required: Destination fax number (E.164 format)
FAX_TO=+15551234567

# Optional: Worker URL (defaults to https://YOUR-WORKER-NAME.workers.dev)
WORKER_URL=https://YOUR-WORKER-NAME.workers.dev

# Optional: Basic auth credentials (if worker has auth enabled)
WORKER_USERNAME=your-username
WORKER_PASSWORD=your-password

# Optional: Path to PDF file (defaults to test-data/sample.pdf)
FAX_FILE=test-data/sample.pdf
